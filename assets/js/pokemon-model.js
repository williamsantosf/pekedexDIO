
class Pokemon {
    number;
    name;
    type;
    types = [];
    photo;
}

//Aqui é criada uma classe pokemon que será utilizada para guardar os valores vindos da API, para que não seja necessário utilizar de forma muito 
//profunda os atribubutos vindos da API. Ou seja, nesse caso estamos criando um modelo próprio para receber os dados vindos da API.